import { Typography } from "@mui/material";

const Timeout = () => {
    return (
        <>
            <Typography variant="body1"
                color="textSecondary"> Se terminó tu tiempo :( !!</Typography>
        </>
    )
};

export default Timeout;